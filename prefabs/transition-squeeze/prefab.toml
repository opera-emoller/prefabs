# Prefab manifest — a derived transition. Inherits everything from
# transition-base and overrides only the fragment shader.
name = "Transition Squeeze"
description = "A squeeze room transition — the outgoing room squeezes vertically to a line with a chromatic-aberration smear as the incoming room appears."
category = "effects"
tags = ["effect:transition", "effect:squeeze", "role:library"]

[parent-link]
# Pinned source; resolves once transition-base is published + tagged.
source = "gh:opera-emoller/prefabs/transition-base@0.1.0"
