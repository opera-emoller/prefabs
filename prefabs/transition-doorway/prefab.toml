# Prefab manifest — a derived transition. Inherits everything from
# transition-base and overrides only the fragment shader.
name = "Transition Doorway"
description = "A doorway room transition — the outgoing room splits open down the middle while the incoming room zooms in behind it, over a floor reflection."
category = "effects"
tags = ["effect:transition", "effect:doorway", "role:library"]

[parent-link]
# Pinned source; resolves once transition-base is published + tagged.
source = "gh:opera-emoller/prefabs/transition-base@0.1.0"
