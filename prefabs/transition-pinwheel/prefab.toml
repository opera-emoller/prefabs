# Prefab manifest — a derived transition. Inherits everything from
# transition-base and overrides only the fragment shader.
name = "Transition Pinwheel"
description = "A pinwheel / clock-wipe room transition — a rotating wedge sweep between rooms."
category = "effects"
tags = ["effect:transition", "effect:pinwheel", "role:library"]

[parent-link]
# Pinned source; resolves once transition-base is published + tagged in the catalog.
source = "gh:opera-emoller/prefabs/transition-base@0.1.0"
