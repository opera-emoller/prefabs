# Prefab manifest — a derived transition. Inherits everything from
# transition-base and overrides only the fragment shader.
name = "Transition Rotate Scale Fade"
description = "A rotate-scale-fade room transition — spins and zooms the frame while cross-fading between the outgoing and incoming rooms."
category = "effects"
tags = ["effect:transition", "effect:rotate-scale-face", "role:library"]

[parent-link]
# Pinned source; resolves once transition-base is published + tagged.
source = "gh:opera-emoller/prefabs/transition-base@0.1.0"
