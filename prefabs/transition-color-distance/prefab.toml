# Prefab manifest — a derived transition. Inherits everything from
# transition-base and overrides only the fragment shader.
name = "Transition Color Distance"
description = "A colour-distance room transition — pixels switch based on the colour difference between the two rooms, then fade to the incoming room."
category = "effects"
tags = ["effect:transition", "effect:color-distance", "role:library"]

[parent-link]
# Pinned source; resolves once transition-base is published + tagged.
source = "gh:opera-emoller/prefabs/transition-base@0.1.0"
